{% set pb_style = settings.pb_style | default('1') %}
{% set progress_map = {'1': 'line', '2': 'circle', '3': 'dot'} %}
{% set progress_type = progress_map[pb_style] | default('line') %}

{# Build inline CSS custom properties for dynamic values #}
{% set inline_style = '' %}
{% if pb_style != '1' and settings.pb_size is defined and settings.pb_size %}
    {% set inline_style = inline_style ~ '--pb-size:' ~ settings.pb_size ~ 'px;' %}
{% endif %}
{% if pb_style == '3' and settings.pb_border_width is defined %}
    {% set inline_style = inline_style ~ '--pb-border-width:' ~ settings.pb_border_width ~ 'px;' %}
{% endif %}
{% if settings.pb_percentage_color is defined and settings.pb_percentage_color is not empty %}
    {% set inline_style = inline_style ~ '--pb-percentage-color:' ~ settings.pb_percentage_color ~ ';' %}
{% endif %}

<div class="wcf__progressbar style-{{ pb_style }} {{ settings.classes | join(' ') }}"
     data-id="{{ id }}"
     data-e-type="{{ type }}"
     data-pb-type="{{ progress_type }}"
     data-pb-percentage="{{ settings.pb_percentage | default(50) }}"
     data-pb-color="{{ settings.pb_color | default('#7DDED8') }}"
     data-pb-bg-color="{{ settings.pb_bg_color | default('') }}"
     data-pb-stroke-width="{{ settings.pb_stroke_width | default(2) }}"
     data-pb-trail-width="{{ settings.pb_trail_width | default(1) }}"
     data-pb-display-percentage="{{ settings.pb_display_percentage ? 'true' : 'false' }}"
     {% if inline_style is not empty %}style="{{ inline_style }}"{% endif %}
     {% if settings._cssid is defined and settings._cssid is not empty %}id="{{ settings._cssid | e('html_attr') }}"{% endif %}
     {% if settings.attributes is defined and settings.attributes is not empty %}{{ settings.attributes | raw }}{% endif %}
     {{ editor_attributes | default('') | raw }}>

    {% if pb_style == '3' %}
        <div class="progressbar dots">
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
        </div>
    {% else %}
        <div class="progressbar"></div>
    {% endif %}

</div>
