{% set classes = ['aae-a-image-compare', 'e-con', 'e-atomic-element', base_styles.base] | merge(settings.classes | default([])) | join(' ') %}
{% set default_position = settings.default_position | default(50) %}
{% set direction = settings.direction | default('horizontal') %}
{% set click_move = (settings.enable_click_move is defined and settings.enable_click_move == false) ? 'no' : 'yes' %}

<div class="{{ classes }} {{ editor_classes | default('') }}"
    data-id="{{ id }}"
    data-element_type="{{ type }}"
    data-e-type="{{ type }}"
    data-interaction-id="{{ interaction_id | default('') }}"
    data-default-position="{{ default_position }}"
    data-direction="{{ direction }}"
    data-enable-click-move="{{ click_move }}"
    style="--aae-image-compare-position: {{ default_position }}%;"
    {% if settings._cssid is defined and settings._cssid is not empty %}
        id="{{ settings._cssid | e('html_attr') }}"
    {% endif %}
    {% if settings.attributes is defined and settings.attributes is not empty %}
        {{ settings.attributes | raw }}
    {% endif %}
    {{ editor_attributes | default('') | raw }}>

    {#
        No inline <style> here — per-element look (position, sizing,
        object-fit, clip-path, colors, layout) lives in each preset's own
        local element styles (Widgets/ImageCompare/presets/*.json), same as
        Btn/SocialShare. Only the live CSS-var-driven slider position, the
        sibling-combinator hover effect, and the vendor pseudo-elements —
        things no preset/schema can express — live in
        assets/scss/image-compare.scss (enqueued via `aae-a-image-compare-css`).
        The one inline `style` attribute above is the dynamic per-render
        seed value for that CSS var, not a style rule.
    #}

    <label class="aae-a-image-compare__label" for="aae-image-compare-range-{{ id }}">Move image comparison slider</label>
    <input type="range"
           min="0"
           max="100"
           value="{{ default_position }}"
           class="aae-a-image-compare__range"
           id="aae-image-compare-range-{{ id }}"
           data-aae-compare-range>

    {{ children_placeholder | raw }}
</div>
